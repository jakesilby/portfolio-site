const caseHeaderScript = document.currentScript;

caseHeaderScript.insertAdjacentHTML('afterend', `
<header class="case-header">
  <a class="case-header__back type-label-secondary" href="/">← WORK</a>
</header>
`);
